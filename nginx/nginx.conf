events {}

http {
    server {
        listen 80;

        location /data/ {
            root /usr/share/nginx/html;
            add_header Access-Control-Allow-Origin *;
        }

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
